specVersion: "v1beta"
serviceId: "journalapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "journalapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
